# -*- mode: snippet -*-
# name: module
# key: module
# group : chisel
# --

class ${1:mymodule} extends Module {
  val io = IO(new Bundle{
    val ${2:name} = Input(${3:Bool()})
    ${4:val ${5:name} = Input(${6:UInt(${7:8}.W)})}
  })
  /**
   *${8:Module docstring}
   */
  $0
}